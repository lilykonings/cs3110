(* Charles Tark (cyt25) and Lillian Chen (qc53) *)
(* October 2, 2014 *)

open Parser
open Quadtree

let load_city_data (s:string) : string quadtree =
	let rec build_quadtree (q: quadtree) (cities: city list) =
		| [] ->
		| 

let city_search (q: string quadtree) (r : region) : string list = 
	failwith "TODO"
